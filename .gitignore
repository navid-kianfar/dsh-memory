# Build output — reproduced by `pnpm run build` (tsc emit → tsdown bundle).
lib/
types/
tsbuild/
*.tsbuildinfo

# Packing artifacts
*.tgz

# Dependencies
node_modules/

# OS noise
.DS_Store
Thumbs.db

# NOTE: `generated/` is deliberately NOT ignored. It holds the Typert RPC
# contract, rendered from the hand-maintained table in scripts/typert-endpoints.mjs
# rather than by the deepseek-harness generator — it ships as committed source.
# `pnpm test` fails if it drifts from src/host/ or from that table; after
# updating the table, run scripts/regen-typert.mjs to regenerate and re-record.

# JetBrains
.idea/
